name: deploy site
on:
 push:
  branches:
   -main
jobs:
 deploy:
  run-on: ubuntu-latest
  
  steps:
   - name: checkout
     uses: actions/checkout@v4

   - name: deploy
     run: echo "Deploying website..."