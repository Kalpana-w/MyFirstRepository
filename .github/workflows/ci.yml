name: CI Pipeline

on:
  pull request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: checkout
        uses: actions/checkout@v4

      - name: run test
        run: echo "CI working 🚀"